import {Cron} from '@90soft/parse-server-kit';

/**
 * Cron jobs — add scheduled tasks here using the @Cron decorator.
 *
 * Example:
 *
 * class CronJobs {
 *   @Cron({
 *     schedule: '0 * * * *',          // every hour
 *     description: 'Clean up expired sessions',
 *     enabled: true,
 *   })
 *   static async cleanExpiredSessions() {
 *     // your logic here
 *   }
 * }
 */
class CronJobs {
  // Add your cron jobs here
}
